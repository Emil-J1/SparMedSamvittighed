export interface FoodWasteData {
    // Define the structure of your API response
    // Adjust these fields according to your actual API response structure
    // Example fields:
    id: number;
    name: string;
    // Add more fields as needed
}